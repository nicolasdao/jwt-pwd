{
  "name": "jwt-pwd",
  "version": "0.3.2",
  "description": "Tiny encryption helper to manage JWT tokens and encrypt and validate passwords using methods such as md5, sha1, sha256, sha512, ripemd160.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/nicolasdao/jwt-pwd.git"
  },
  "bugs": {
    "url": "https://github.com/nicolasdao/jwt-pwd/issues"
  },
  "homepage": "https://github.com/nicolasdao/jwt-pwd#readme",
  "scripts": {
    "dev": "NODE_ENV=dev node-dev index.js",
    "lint": "eslint index.js src/ test/ --fix",
    "push": "git push --follow-tags origin master && npm publish",
    "rls": "standard-version --release-as",
    "start": "NODE_ENV=production node index.js",
    "test": "mocha",
    "v": "node -e \"console.log(require('./package.json').version)\""
  },
  "author": "Nicolas Dao",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^4.19.1",
    "mocha": "^5.2.0",
    "standard-version": "^4.4.0"
  },
  "dependencies": {
    "jsonwebtoken": "^8.5.0",
    "node_hash": "^0.2.0"
  }
}
